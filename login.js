(async()=>{try{const r=await fetch('/api/gateway?op=session',{cache:'no-store'});if(r.ok)location.replace('/api/gateway?op=dashboard');}catch{}})();
document.getElementById('login').addEventListener('submit',async e=>{e.preventDefault();const button=document.getElementById('submit'),password=document.getElementById('password');button.disabled=true;document.getElementById('status').textContent='';try{const r=await fetch('/api/gateway?op=login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:document.getElementById('username').value,password:password.value})});const data=await r.json();password.value='';if(!r.ok)throw new Error(data.error||'Sign-in failed.');location.replace('/api/gateway?op=dashboard');}catch(error){document.getElementById('status').textContent=error.message;}finally{button.disabled=false;}});
